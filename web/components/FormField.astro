---
interface Props {
  label: string;
  for: string;
  hint?: string;
}
const { label, for: id, hint } = Astro.props;
---

<div>
  <label for={id} class="usa-label mt-0 font-bold text-ink-950">{label}</label><slot />{hint && <p class="usa-hint mt-1.5 text-sm leading-5">{hint}</p>}
</div>
